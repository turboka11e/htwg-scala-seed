package de.htwg.se.Carcassonne.model.fileIOComponent.fileXmpImpl

import org.scalatest._

class FileIOSpec extends WordSpec with Matchers {

  "+++ Blueprint +++" should {
    val test = 1
    "+++ Blueprint +++" in {
      test should be(2)
    }
  }
}
